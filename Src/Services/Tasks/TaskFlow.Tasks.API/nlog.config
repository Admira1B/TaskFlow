<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      throwConfigExceptions="true">

	<variable name="service-name" value="tasks-service" />

	<targets>
		<target name="logs-file"
				xsi:type="File"
				fileName="${basedir}/logs/${service-name}_${shortdate}.log"
				archiveFileName="${basedir}/logs/archive/${service-name}_${shortdate}.log"
				archiveEvery="Day"
				maxArchiveFiles="31"
				encoding="utf-8">

			<layout xsi:type="JsonLayout" includeAllProperties="true">
				<attribute name="level" layout="${level:uppercase=true}" />
				<attribute name="logger" layout="${logger:shortName=true}" />
				<attribute name="message" layout="${message}" />
				<attribute name="exception" layout="${exception:format=ToString}" />
				<attribute name="timestamp" layout="${date:format=yyyy-MM-ddTHH\:mm\:ss.fffZ}" />
			</layout>
		</target>

		<target name="console" xsi:type="ColoredConsole" useDefaultRowHighlightingRules="false"
				layout="${date:format=HH\:mm\:ss} | ${level:uppercase=true} | ${logger:shortName=true} | ${message}">

			<highlight-word foregroundColor="White" ignoreCase="true" text="TRACE" />
			<highlight-word foregroundColor="DarkGray" ignoreCase="true" text="DEBUG" />
			<highlight-word foregroundColor="Green" ignoreCase="true" text="INFO" />
			<highlight-word foregroundColor="Yellow" ignoreCase="true" text="WARN" />
			<highlight-word foregroundColor="Red" ignoreCase="true" text="ERROR" />
			<highlight-word foregroundColor="Red" backgroundColor="White" ignoreCase="true" text="FATAL" />
		</target>
	</targets>

	<rules>
		<logger name="tasks-service" minlevel="Info" writeTo="console" />
		<logger name="tasks-service" minlevel="Debug" writeTo="logs-file" />

		<logger name="TaskFlow.Tasks.*" minlevel="Info" writeTo="console" />
		<logger name="TaskFlow.Tasks.*" minlevel="Debug" writeTo="logs-file" />

		<logger name="*" minlevel="Error" writeTo="console" />
		<logger name="*" minlevel="Warn" writeTo="logs-file" />
	</rules>
</nlog>